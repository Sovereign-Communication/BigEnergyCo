{
  "tests_green": true,
  "prettier_clean": true,
  "seo_green": true,
  "smoke_green": true,
  "ci_green": true,
  "tests_summary": "689/689 npm test green (node --test tests/*.test.mjs); coverage gate green (lines 67.46>=66, branches 78.35>=76, funcs 60.00>=60); deploy:check stages the allowlisted build clean",
  "prettier_clean_summary": "prettier 3.9.6 --check repo-wide: all matched files use Prettier code style (exit 0)",
  "seo_summary": "npm run seo and node scripts/check-chars.mjs exit 0: JSON-LD, sitemap, metadata, characters, quality, i18n (parity, hooks, placeholders, rendered keys), CSP/headers, country files, asset tokens 20260921f",
  "smoke_note": "local browser smoke 115/115 gates PASSED on the allowlisted build under the real _headers policy: grid-tie and off-grid runs, the infeasible banner shown then retracted by the solvable run that replaced it, weather persistence with zero warm re-pulls, share-restore plus a mid-run reload keyed to performance.timeOrigin, run-deadline retry, Jev badge lifecycle, a11y flow (keyboard 64/81/47 stops, AA contrast, reduced motion), zero console or CSP errors",
  "advisor_audit": "the same selected deterministic pair feeds Jev and the prose; pending/unavailable/available framing is explicit and a pass is never called safe or certified; injection, fake instructions and secret-reveal are ignored; no exact coordinates in the brief; uncertainty and safety caveats mandatory; chemistries conditional, never a universal winner; 429/5xx degrade to sanitized textContent prose across six locales and RTL",
  "notes": [
    "the design pass targeted what the judge's own rubric names: one owner per piece of state, concerns in separate modules, no grown app left in one giant file",
    "the duplicate string contract was removed rather than documented: ui.js binds translate as t, so the next substitution fix lands once",
    "the parts-list move is proven by re-running the pre-refactor row builder from git HEAD against the new module on three fixtures: byte-identical CSV",
    "a live page-to-page CSV comparison was not usable as proof because the two sessions had sized different systems - the numbers differed, not the formatting",
    "deleting the in-file reason table missed two further consumers, which a 115-gate browser smoke caught as a hard ReferenceError; the print sheet had always rendered the word undefined there",
    "gate run provider=typesafe direct (key resolved read-only from ~/.config/harness/jev.env); no secrets, raw user messages or exact coordinates recorded here"
  ],
  "facet_evidence": {
    "spec": "free worldwide estimator, zero runtime dependencies, no paywall or lead capture; this pass ships ownership extractions with their tests - no unrequested surface, no new capability",
    "design": "one owner per piece of state: run-coordinator (run), share-codec (share links), infeasible-copy (reason to copy keys), parts-csv (parts-list CSV), interpolate (the placeholder contract ui.js and i18n.js each duplicated); ui.js 8167 to 8014 lines",
    "correctness": "real-browser exercise of the infeasible and parts-list paths; the pre-refactor row builder re-run from git HEAD against the new module on three fixtures produced byte-identical CSV; 689 tests green",
    "quality": "prettier clean repo-wide; the one piece of genuine duplication the design audit found was removed instead of documented - substitution now has a single implementation, so its next fix lands once",
    "security": "tracked-secret scan clean, .env ignored, CSP and headers green (no unsafe-inline or eval, script-src closed over 82 pages), advisor receives no exact coordinates, output via textContent only",
    "performance": "warm same-location reload issues zero NASA pulls (smoke-proven), payload-cache repeats are instant, the Jev badge re-mounts with zero refetch, SW cache I/O bounded at 1.5s off the request path",
    "accessibility": "keyboard walks 64/81/47 stops with wrap, every visible control is named, WCAG AA contrast verified in-browser, reduced motion honored; the infeasible reason mirrors into a persistent sr-only polite region",
    "i18n": "42 orphaned keys wired through t()/becoT, a corrupted Arabic advisorTitle repaired, runtime copy now renders through the same translate() the markup uses; parity, placeholders and RTL pinned",
    "seo": "sitemap, JSON-LD, metadata, character and canonical checks green; npm run seo exit 0 and the asset stamp is unchanged at 20260921f",
    "resilience": "three-minute run deadline: a silent worker surfaces an actionable timeout and recovers on an explicit fresh-worker retry (smoke-proven); retry/timeout budget nesting and transient classification are covered by their own suites",
    "experience": "staged pipeline stepper with per-chunk progress and honest speed notes in the visitor's language; timeout errors name the next click; infeasibility reasons say why and what to change, localized in six locales",
    "docs": "ARCHITECTURE names the new owners (interpolate, infeasible-copy, parts-csv) and keeps the extraction seams plus remaining debt current; the runbook gate table matches CI; the GROQ audit is prompt version 2026-09c",
    "testing": "689 tests plus coverage thresholds, mutation-proven gate contracts and 115 browser smoke gates; new this pass: a byte-exact CSV golden captured from the pre-extraction implementation, and per-locale proof every reason has copy",
    "release": "PR #128 through the protected path with Tests, CodeQL and analyze all green, browser smoke green locally on the allowlisted build; the tree is dirty until this pass lands, so tree_clean is expected red",
    "advisor": "adversarial suite green: injection and fake instructions ignored, a stale verdict is rejected after a selection change, provider failure falls back silently, locale allow-list enforced, reviewed state paired with prose",
    "physics": "sizing stays deterministic from five years of hourly NASA POWER (contract tests); chemistry gates intact (sodium/LFP trade-off, lead-acid no balancing, cold floors, AGM below LFP, sodium premium); verify:live PASSED"
  }
}
